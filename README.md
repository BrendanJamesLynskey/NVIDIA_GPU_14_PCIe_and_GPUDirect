# PCIe & GPUDirect — The Bus Every GPU Sits On

PCIe generations (3 to 6), Resizable BAR, IOMMU and ACS, NUMA pinning, GPUDirect P2P / RDMA / Storage — the system-board layer that decides whether your multi-GPU box actually scales, with practical diagnostics (`nvidia-smi topo -m`, `lspci`) and an interactive PCIe topology lint that flags the common failure modes.

**Live site:** https://brendanjameslynskey.github.io/NVIDIA_GPU_14_PCIe_and_GPUDirect/

Part of the [NVIDIA GPU Architectures series](https://github.com/BrendanJamesLynskey/LLMs#nvidia-gpu-architectures).
